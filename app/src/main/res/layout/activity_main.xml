<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:="@drawable/wallpapers_a_02"
    android:background="@drawable/wall11">

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:="@drawable/logo"
        android:id="@+id/imageView"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="80dp"
        android:scrollbarThumbHorizontal="@drawable/wall11"
        android:scrollbarThumbVertical="@drawable/wall11"
        android:background="@drawable/logo11" />

    <Button
        android:text="Search"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="clickSearch"
        android:id="@+id/button"
        android:layout_alignParentBottom="true"
        android:background="@android:drawable/divider_horizontal_textfield" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="textPersonName"
        android:ems="10"
        android:id="@+id/editText"
        android:hint="@string/search"
        android:textColor="?attr/colorButtonNormal"
        android:textColorLink="@color/colorPrimaryDark"
        android:layout_below="@+id/imageView"
        android:layout_centerHorizontal="true"
        android:textColorHint="@color/colorPrimary"
        android:layout_marginTop="20dp" />
</RelativeLayout>
